# 智能体操作手册 · Lectures & Échos 知识库站点

> 本手册面向「接手本仓库的任何智能体（或人类协作者）」，把这个项目里**所有可能出现的工作**——从微信读书同步、笔记修复、内容新增、封面制作，到本地构建、浏览器测试、线上部署、故障排查——全部写成可照做、可复跑的标准工作流（SOP）。
>
- **仓库根目录**：`C:\Users\NewtN\下载\lectures-et-echos-main`（下文用 `<ROOT>` 代指）
- **站点目录**：`<ROOT>\site`（Astro 7 静态站点，下文用 `<SITE>` 代指）
- **书摘库**：`<ROOT>\微信读书\`（微信读书导出的 Markdown）
- **最后更新**：2026-09-23
- **铁律**：动手前先读「第 2 章 操作总原则」；任何写入类操作，先 dry-run / 先备份、后落盘；交付前必须跑完「第 11 章 验收清单」。
- **最高内容准则（用户钦定）**：**不管是更新内容、添加内容，还是整合内容，都必须尊重微信读书所有内容在网站上的「完整性」与「一手性」**——详见 §2.0，任何时候不得违反。

---

## 目录

1. [项目是什么：架构与数据流](#1-项目是什么架构与数据流)
2. [操作总原则（每次任务都先读）](#2-操作总原则每次任务都先读)
3. [环境准备与本地命令](#3-环境准备与本地命令)
4. [工作流 A：微信读书全量 / 增量同步](#4-工作流-a微信读书全量--增量同步)
5. [工作流 B：笔记修复、想法找回、重名书合并](#5-工作流-b笔记修复想法找回重名书合并)
6. [工作流 C：站点构建、预览与浏览器测试](#6-工作流-c站点构建预览与浏览器测试)
7. [工作流 D：新增 / 编辑内容（博主文章、专题、合集）](#7-工作流-d新增--编辑内容博主文章专题合集)
8. [工作流 E：封面与图片（WebP 硬性规范）](#8-工作流-e封面与图片webp-硬性规范)
9. [「我的书架」模块原理与改造点](#9-我的书架模块原理与改造点)
10. [工作流 F：部署到 GitHub Pages](#10-工作流-f部署到-github-pages)
11. [交付前验收清单（逐项打勾）](#11-交付前验收清单逐项打勾)
12. [常见故障与踩坑手册](#12-常见故障与踩坑手册)
13. [关键文件地图](#13-关键文件地图)

---

## 1. 项目是什么：架构与数据流

### 1.1 一句话说明

仓库根目录下的**每一个中文文件夹 = 一个「集合（collection）」**：

- `微信读书\` 是**书库（library）**：每本书一个 md（划线 + 想法 + 元数据），另有阅读统计、Gallery；
- 其余文件夹（`毒舌的南瓜\`、`渤海小吏\`、`安森垚\` …）是**创作者（creator）/ 专题（series）**：每篇视频文稿 / 文章一个 md；
- `<SITE>` 是一个 **Astro 7 纯静态站点**：构建时扫描仓库里所有 md，渲染成首页双模块（**我的书架 / 网络博主**）、集合页、专题页、文章详情页。

### 1.2 数据流（从原始资料到网页）

```
微信读书云端 API
   │  （Agent Gateway，见 §4.2）
   ▼
临时导出目录 .weread-export/
   ├─ export-manifest.json   （failureCount 必须为 0）
   ├─ books/<bookId>/data.json
   └─ reading-stats/overall.json + monthly/*.json
   │  node scripts/import-weread-export.mjs <导出目录>
   ▼
<ROOT>\微信读书\*.md           ← 唯一的「事实源（source of truth）」
   │  Astro 构建时 glob 扫描（content.config.ts）
   ▼
src/lib/catalog.ts            （全部 md → 统一 Note 目录模型）
   ├─ src/lib/bookshelf.ts    （只挑「微信读书」书 → 我的书架）
   └─ components/modules/*    （书架模块 / 博主模块）
   │  astro build
   ▼
<SITE>\dist\                  （纯静态 HTML，可直接部署 / 本地起服预览）
```

**关键认知**：

1. **源文件是根目录的 md，不是站点数据库。** 想改内容，改 md；想改展示，改 `src/`。不要手改 `dist/`（构建即覆盖）。
2. **书架与博主严格分离**：只有 frontmatter 里 `doc_type: weread-highlights-reviews`、且位于 `微信读书\` 的 md 才进「我的书架」；`weread-reading-stats` 是统计页（不进书架网格）；其余全部进「网络博主」。
3. **展示层规则不回写源文件**：例如缺 `category` 的书，由 `bookshelf.ts` 的兜底关键词规则在内存里归类，绝不改写 md。

### 1.3 目录结构速览

```text
<ROOT>/
├─ 微信读书/                 # 书库：<书名>.md / <书名>-<bookId>.md + 阅读统计 + Gallery
│   └─ （个别书的）文本附件/    # 该书本地封面（WebP）
├─ 毒舌的南瓜/、渤海小吏/ …    # 创作者 / 专题集合，结构与书库相同
├─ AI 指令/                  # 18 个整理资料用的小型提示词（不进站点，被 glob 排除）
├─ README.md
├─ .gitattributes            # 统一 LF 换行（2026-09 新增）
├─ .github/workflows/pages.yml   # GitHub Pages 自动部署
├─ 启动网站.cmd               # Windows 双击启动 dev（依赖 pnpm，见 §3.4 坑）
└─ site/                     # ↓ Astro 站点
   ├─ astro.config.mjs       # site/base、markdown 管线（remark-obsidian）
   ├─ package.json           # 脚本与依赖（Node >=24 <25）
   ├─ scripts/               # 4+2 个运维脚本（见 §13）
   ├─ src/
   │  ├─ content.config.ts   # md glob 范围 + frontmatter zod schema
   │  ├─ config/collections.ts   # 集合类型（creator/series/library）
   │  ├─ lib/
   │  │  ├─ catalog.ts       # 全库目录模型（核心）
   │  │  ├─ bookshelf.ts     # 书架数据层（排序/分类兜底，核心）
   │  │  ├─ url.ts           # 稳定 ID、base 路径、媒体 URL
   │  │  └─ pinyin.ts        # 拼音排序
   │  ├─ components/modules/ # BookshelfModule / BloggersModule
   │  ├─ pages/              # index、notes/[id]、collections/[id]、topics/[id]、404
   │  ├─ layouts/、components/、styles/、plugins/
   │  └─ …
   ├─ assets/cover-system/   # 自动封面的母版画 + 清单
   ├─ public/                # 构建时 sync-assets 同步进来的 media/、字体、3d/
   └─ dist/                  # 构建产物（交付物）
```

---

## 2. 操作总原则（每次任务都先读）

### 2.0 最高内容准则：尊重微信读书内容的完整性与一手性（用户钦定，凌驾于整洁偏好之上）

**不管是更新内容（同步 / 修复）、添加内容（新书 / 新笔记），还是整合内容（重名书合并 / 结构调整），都必须保证微信读书的每一条内容在网站上完整、且忠于一手来源。** 具体边界：

1. **唯一事实源是微信读书本身**（云端 API / 官方导出）。书摘 md 与站点页面只是它的镜像：不脑补、不润色、不概括、不“纠错”原文用字；原文是繁体就保留繁体、章节名是 `_5` / `2` 这类导入占位名也原样保留，不用自拟标题替换。
2. **一条都不能少**：划线（📌）、想法（💭 / 独立读书笔记 / 书评）、元数据（进度、时长、日期、评分、ISBN、简介等）只能增补或如实同步，**禁止以“精简 / 重复 / 没价值 / 是广告”为由删除任何微信读书原始内容**（博主文稿的口播广告同理保留，见 §7.1）。
3. **整合 ≠ 删内容**：重名书合并只去掉“同一本书不同副本里的同一条重复笔记”（按 §5.4 判据），**独特笔记取并集、一条不丢**；合并后计数必须等于各副本去重后的总和，交付时逐条核对。
4. **不同译本 / 版本 / 文种是不同的书，不得合并**：译者、出版社、ISBN 不同（如《培根随笔》蒲隆 / 陶文佳译本、《魔戒》邓嘉宛 / 朱学恒译本与英文原版、《只是为了好玩》旧译本与陈少芸修订版、Harry Potter 中/英文版），哪怕同名也各自保留；“同一部作品的繁 / 简两个版本”（如《国史大纲》九州繁体本与简体导入本）才属于可合并的同书副本——拿不准时用 API 章节结构 + 划线重叠度判定，并在交付说明里写明依据。
5. **展示层的归类 / 排序 / 兜底不回写源文件**（§1.2 已述）；任何让站点内容与微信读书不一致的“美化”都不允许。发现源数据疑似异常时，先重新拉 API 核对，而不是手工改 md。

### 2.1 六步工作法

任何任务都按 **探查 → 约束 → 证据 → 执行 → 验证 → 交付** 六步走：

1. **探查（先读后动）**
   - 改任何文件前先 `Read` 它当前的真实内容，**不凭记忆/印象改**；脚本契约记不清就重读脚本头注释。
   - 数字必须有来源：来自输入文件、API 回包或可复现计算；估算要标注口径。
2. **约束（守住边界）**
   - 用户点名的技术栈、目录、格式、交付物是硬约束；只改被点名范围，不动无关文件。
   - 历史遗留文件（如 `微信读书\微信读书-导出清单.csv`、`微信读书-导出索引.json`）模块不依赖、用户没让动，就**保持原样**。
   - 不向未授权的外部账号 / 仓库写入。
3. **证据（先验证假设）**
   - 调微信读书接口前，先读 `C:\Users\NewtN\.agents\skills\weread-skills\` 下对应说明（shelf/notes/book/readdata），确认参数名、单位、分页方式。
4. **执行（小步、可逆、可回滚）**
   - 修复类脚本一律**先 `--dry-run` 看报告，再 `--write`**。
   - 批量改 md 前，可用 git 挂起改动（`git status` 先看工作区），方便 `git checkout -- <file>` 回滚。
5. **验证（用不同于生成的路径回读）**
   - 数据层：跑一致性脚本（§11）；构建层：四步全过；展示层：起本地静态服 + 浏览器真点一遍（§6.3）。
   - “文件存在 / 语法通过”不算完成，**用户真正看到的页面正确**才算。
6. **交付（用户拿得到）**
   - 最终回复写清：改了什么、产物在哪、怎么打开、验证结果、剩余缺口。
   - 生成 / 改动的最终文件通过正规交付通道呈现，不把中间临时目录当交付物。

---

## 3. 环境准备与本地命令

### 3.1 运行时要求

- **Node.js 24**（`package.json` engines：`>=24 <25`，本机实测 v24.15.0）。
- 包管理器声明为 **pnpm 11.9.0**，但**本机 pnpm 已损坏**（corepack shim 缺 `pnpm.cjs`）。
  - ✅ 统一做法：**所有脚本用 `node` 直接跑，Astro CLI 用 `.\node_modules\.bin\astro.cmd`**，不要用 pnpm/npm/npx 去联网装东西。
  - `<SITE>\node_modules` 已安装好，无需重装；确需重装时在 `<SITE>` 内 `npm install`（不要装到全局）。

### 3.2 微信读书 API 密钥

- 环境变量 **`WEREAD_API_KEY`**（`wrk-` 开头），已在用户环境配置；调用时从 `os.environ` 读取。
- 新开终端若读不到，PowerShell 临时设置：`$env:WEREAD_API_KEY="wrk-xxxx"`。
- 密钥绑定账号身份，**不要写进任何文件、不要打印到交付物里**。

### 3.3 package.json 脚本与「node 直跑」对照

| 目的 | 声明命令（pnpm，本机勿用） | 本机实际命令（在 `<SITE>` 下） |
| --- | --- | --- |
| 开发预览（热更新） | `pnpm dev` | `node .\scripts\sync-assets.mjs; .\node_modules\.bin\astro.cmd dev` |
| 导入微信读书导出 | `pnpm import:weread -- <目录>` | `node .\scripts\import-weread-export.mjs <目录>` |
| 补缺失封面 | `pnpm generate:covers` | `node .\scripts\generate-missing-covers.mjs` |
| **封面完整性检查** | `pnpm check:covers` | `node .\scripts\generate-missing-covers.mjs --check`（只报告，不写入；有问题 exit 1） |
| 同步图片资源 | `pnpm sync-assets` | `node .\scripts\sync-assets.mjs` |
| 类型检查 | `pnpm check` | `.\node_modules\.bin\astro.cmd check` |
| **完整构建（四合一）** | `pnpm build` | 依次跑 §6.1 四条命令 |
| 预览构建产物 | `pnpm preview` | `.\node_modules\.bin\astro.cmd preview` |

> 笔记修复脚本 `repair-weread-notes.mjs` **没有**注册进 package.json，直接 `node .\scripts\repair-weread-notes.mjs …`（见 §5）。

### 3.4 Windows / PowerShell 硬性注意事项（踩过的坑，务必遵守）

1. **不要用 PowerShell 的 `curl` 发 JSON**：它会给 body 加 UTF-8 BOM，网关报 `errcode -2007`。调微信读书 API **一律用 Python `urllib`**（§4.2 有模板）。
2. **不要用 `python -c "..."` 内联复杂代码**：PowerShell 会破坏单引号 / 中文字符。**把 Python 写成 `.py` 文件再 `python -X utf8 脚本.py`**。
3. **PowerShell 对中文文件名的 `Where-Object` / 通配过滤不可靠**（经常静默返回空）。统计、筛选、改名中文文件**一律用 Python `os`/`re`**。
4. 所有 Python 脚本加 `-X utf8`，避免控制台 GBK 报错。
5. 命令超时会被自动转入后台任务，用任务输出工具等它结束，不要重复发起。

---

## 4. 工作流 A：微信读书全量 / 增量同步

> 适用：用户说「同步微信读书」「更新书架」「把新书摘导进来」「刷新阅读统计」。
> 全量约 200 本 × 4 接口，耗时 2–4 分钟；增量只抓新/变动书可更快，但**导入器按 bookId 幂等覆盖**，全量重跑也安全。

### 4.1 总流程

```
① /shelf/sync 拿书架书目  →  ② /user/notebooks 拿笔记本清单（游标分页）
③ 对每本书抓 4 个接口，写 books/<id>/data.json
④ /readdata 总览 + 逐月统计
⑤ 写 export-manifest.json（failureCount 必须 =0，失败的补抓到 0）
⑥ node import-weread-export.mjs 把导出目录写成 md
⑦ 一致性校验（§11）→ ⑧ 构建四步（§6.1）→ ⑨ 浏览器冒烟（§6.3）
```

### 4.2 网关调用规范（先读 weread-skills）

- **入口**：`POST https://i.weread.qq.com/api/agent/gateway`
- **Header**：`Authorization: Bearer %WEREAD_API_KEY%`、`Content-Type: application/json`
- **Body**：JSON，`api_name`（**必须带前导斜杠**，如 `/shelf/sync`，漏了会 499）、业务参数、`"skill_version":"1.0.4"` **三者平铺在同一层**；禁止 `params` 嵌套。
- 回包 `errcode != 0` 即错误；出现 **`upgrade_info` 必须立即暂停**，按其 message 升级 skill 版本后再继续。
- 单请求约 0.5s，未见限流；网络重置类错误**重试 3 次（指数退避）**。

Python 调用模板（存成 `.py` 跑，勿内联）：

```python
import json, os, time, urllib.request
KEY = os.environ['WEREAD_API_KEY']
def call(payload, timeout=40, retries=3):
    payload = {**payload, 'skill_version': '1.0.4'}
    last = None
    for i in range(retries):
        try:
            body = json.dumps(payload).encode('utf-8')
            req = urllib.request.Request(
                'https://i.weread.qq.com/api/agent/gateway', data=body, method='POST',
                headers={'Authorization': 'Bearer ' + KEY, 'Content-Type': 'application/json'})
            with urllib.request.urlopen(req, timeout=timeout) as r:
                d = json.loads(r.read().decode('utf-8'))
            if d.get('errcode'): raise RuntimeError(f"{d.get('errcode')} {d.get('errmsg')}")
            return d
        except Exception as e:
            last = e; time.sleep(1.5 * (i + 1))
    raise last
```

### 4.3 各接口与口径（细节以 weread-skills/*.md 为准）

| 环节 | api_name | 关键参数 / 分页 | 用途 |
| --- | --- | --- | --- |
| 书架 | `/shelf/sync` | — | 书架总量 = `books.length + albums.length + (mp 非空?1:0)` |
| 笔记本清单 | `/user/notebooks` | `count` + `lastSort` **游标翻页**（取到无下一页） | 哪些书有笔记、noteCount/reviewCount/bookmarkCount |
| 章节 | `/book/chapterinfo` | `bookId` | 章节 chapterUid / chapterIdx / title（排序依据） |
| 划线 | `/book/bookmarklist` | `bookId` | `updated[]`：markText、range、chapterUid、createTime |
| 想法/点评 | `/review/list/mine` | **小写 `bookid`**、`count`、`synckey` 翻页取全 | reviews[]：content、range、type、chapterName、createTime |
| 进度 | `/book/getprogress` | `bookId` | `book.{progress(百分比), readingTime(秒), startReadingTime, updateTime, finishTime}` |
| 统计总览 | readdata 总览接口（见 `readdata.md`） | — | overall.json |
| 逐月统计 | readdata 月接口 | 从注册月（本账号 2024-09）到当前月，每月一个 | monthly/YYYY-MM.json |

**单位**：所有时间戳是 Unix 秒，展示转 `YYYY-MM-DD`；阅读时长是秒，展示「X小时Y分钟」。

### 4.4 导出目录契约（导入器硬性要求）

临时目录（建议 `<ROOT>\.weread-export`，**用完删除**）结构：

```text
.weread-export/
├─ export-manifest.json     # 必须含 "failureCount": 0，以及 finishedAt
├─ books/
│   └─ <bookId>/data.json   # 每本一个，聚合下面 4 块：
│                            #   info(书籍信息) / notebook(笔记本项) / shelf(书架项)
│                            #   chapters / highlights / reviews / progress
└─ reading-stats/
    ├─ overall.json
    └─ monthly/2024-09.json … 2026-09.json
```

`data.json` 字段要对齐 `import-weread-export.mjs#renderBook` 的读取：
- `info`（title/author/translator/category/cover/intro/isbn/publisher/publishTime/newRating/deepLink）；
- `notebook`（noteCount/reviewCount/bookmarkCount/sort/book）；
- `progress.book`；`highlights.{updated,chapters}`；`reviews.reviews`；`chapters.chapters`。

> `failureCount != 0` 导入器会**直接报错拒绝导入**。哪本书哪个接口失败，就只补那一本，直到清单归零。

### 4.5 导入（md 生成 / 更新规则）

在 `<SITE>` 下：

```powershell
node .\scripts\import-weread-export.mjs <.weread-export 的绝对路径>
```

行为（读源码确认，勿猜）：

- **按 frontmatter `bookId` 匹配已有文件**：命中 → 覆盖更新；未命中 → 新建 `<安全书名>.md`，若同名文件已占用则 `<安全书名>-<bookId>.md`（这就是重名书带 `-CB_xxx` 后缀的由来）。
- 每本书 md 结构：frontmatter → `# 元数据` callout → `# 高亮划线`（按 `chapterIdx` 排章、章内按 range 起点排序，划线 `> 📌`、时间 `> ⏱`、挂载想法 `> 💭`/`> 🕰`）→ 可选 `# 章节点评与书评`（range 为空的独立想法）。
- **导入器的想法挂载是 range 精确匹配**：review.range 与现存划线 range 对不上的想法会落入「章节点评与书评」；若连章节都没有则可能丢失——这正是 §5 修复脚本要解决的问题。
- 阅读统计写 `微信读书阅读统计.md`（总览/历年/时长最多/偏好/逐月，每日时长折叠）。
- 导入器输出 **LF 换行**。
- 结束会打印：`更新 X 本，新增 Y 本，阅读统计 N 个月`，记录这三个数。

> **fail-closed 规则（2026-09-22 返工加固）**：import 脚本写入前对比旧文件的 📌/💭 计数，若新版本任一更少则跳过该本书、保留旧文件并告警。任一接口（reviews/bookmarklist）失败导致数据残缺时，绝不整体覆盖既有内容。

### 4.6 同步后立刻做

1. 删临时导出目录；
2. 跑 §11 一致性脚本（noteCount/reviewCount 必须全对得上）；
3. 若发现想法对不上 / 重名书，转 §5；
4. 走 §6 构建 + 冒烟；
5. 交付时说明：书架总数、更新/新增数、统计月份范围、累计时长等关键数字。

---

## 5. 工作流 B：笔记修复、想法找回、重名书合并

> 适用：① 某书 frontmatter `reviewCount` 与正文想法数对不上（想法被静默丢弃）；② 同一本书存在多个导入副本（CB_ 开头 bookId、文件名带后缀）需要合并；③ 需要按「书中位置」重排笔记、补齐每条划线时间。
>
> 权威工具是项目自带的 **`site/scripts/repair-weread-notes.mjs`**，**不要自写合并逻辑替代它**。

### 5.1 修复脚本原理（先理解再用）

- **权威源是 API 缓存 JSON**，不是旧 md：它用缓存重建章节/划线/想法模型，再**保留旧 md 的 frontmatter 外壳与元数据 callout**，只替换笔记正文并同步数量/进度/日期。
- 章节按 `chapterIdx` 升序；章内划线按 range 起点（= 书中位置）升序；每条划线补 `⏱`。
- **想法模糊挂载**（解决导入器精确匹配丢想法的问题）：同章节内按 ① range 全等(100 分) → ② 同终点(80 分) → ③ 最大重叠长度 选最佳划线；挂不上的收进「章节点评与书评」的 `## 读书笔记 / ## 本书评论`。
- 公众号（MP_/CB_ 这类无数字 chapterUid）统一归入「正文」单章。

### 5.2 缓存目录与缓存结构（重点，容易错）

脚本写死从**仓库的同级目录**读缓存：

```text
<ROOT>\..\obsidian-weread-plugin-main\weread-export-tool\.cache\<bookId>.json
```

即 `C:\Users\NewtN\下载\obsidian-weread-plugin-main\weread-export-tool\.cache\`（不存在就新建，**用完删除整棵树**）。每个 `<bookId>.json` 结构：

```json
{
  "chapterResp":  { "data": { "updated": [ {"chapterUid":1,"chapterIdx":1,"title":"…"} ] } },
  "highlightResp":{ "updated": [ …bookmarklist 原样… ], "chapters": [ …] },
  "reviewResp":   { "reviews": [ …review/list/mine 全部翻页合并… ] },
  "progress":     { "book": { "progress":38,"readingTime":4025,"startReadingTime":…,"updateTime":…,"finishTime":… } }
}
```

> 注意 `chapterResp.data` 既可能是 `{updated:[…]}` 也可能是 `[{updated:[…]}]`，脚本两种都兼容；其余三个直接放接口原样回包即可。
> **脚本会循环重建「所有有缓存的书」**——所以缓存目录里**只放本次要修的书**，避免误伤其他书。

### 5.3 标准修复步骤

```text
1) 从 md frontmatter 取目标 bookId（中文文件名用 Python 取，勿用 PowerShell 过滤）
2) 对每个 bookId 调 4 接口（§4.2/§4.3），组装成 §5.2 缓存 JSON
3) 先 dry-run：
     node .\scripts\repair-weread-notes.mjs --dry-run [--only=关键词] [--dump]
   读 JSON 报告：rebuilt / totalHighlights / totalReviews / bailu / samples
4) 确认无误再写盘：
     node .\scripts\repair-weread-notes.mjs --write
5) 脚本直接写 LF（早期版本曾写 CRLF，落盘后可按 §5.5 抽查兜底）
6) 删除临时缓存树 …\obsidian-weread-plugin-main
7) 跑 §11 校验 → 构建 → 浏览器抽查详情页
```

参数：`--dry-run` 只报不写；`--write` 落盘；`--only=关键词` 只处理文件名或 bookId 命中的书；`--dump` 把重建后的 Markdown 打到 stdout 供人工核对。

### 5.4 重名 / 多副本合并（MERGE_GROUPS 通用合并）

脚本 `repair-weread-notes.mjs` 用 **`MERGE_GROUPS` 数组**声明所有「同一本书多副本合一」的组（早期版本只有硬编码的白鹿原一组，现已泛化）。每组字段：

- `label`：组名（仅用于报告 / `--only` 过滤）；
- `order`：参与合并的 bookId 顺序。**第 1 个是元数据基准**（其 progress / readingTime / finishedDate 作为合并值），其后依次并入；
- `shellId`：用哪本书现有 md 的 frontmatter + `# 元数据` callout 作为「外壳」（通常选官方数字 id、或信息最全的那本）；
- `outFile`：合并结果写哪个文件名（**缺省 = shellId 现有文件**）；当想让最终文件用「干净无后缀」的名字时显式指定；
- `removeIds`：合并后要 `unlink` 的副本 bookId（注意：若某 id 的现文件正好等于 `outFile`，脚本会自动跳过、不会误删结果）。

合并算法（`mergeModels`，与白鹿原同一套）：

- **按章节名并章**，章节按中文数字（第X章/节/回/卷，兼容「十/百/两」）排序，无序号章排最前；
- **原文去重（两层）**：NFKC 归一化去空白后全等、或双方 ≥10 字且存在包含关系即视为重复（保留更长文本、并集想法、保留更早时间）。① 先做**章内去重**；② 再做**跨章全书级去重**——劣质 PDF 导入副本的章节切分常与好副本不一致（占位章名 `_1/_2`、`2` 等），同一条原文会落在不同章名下，必须跨章再去一次，基准本（`order[0]`）章节优先保留，去空后的占位章不输出；
- 想法按内容去重；元数据取「最晚 lastReadDate、最早 readingDate，其余沿用 order[0]」；
- 主循环会跳过所有出现在任一合并组 `order` 里的 id，这些书只在合并段统一处理。

**前置条件**：`shellId` 的 md 必须存在；`order` 里**每个** bookId 的缓存都必须齐全（缺一个直接报错，避免静默漏并）。

#### 已完成的合并（基准存档）

- 《白鹿原》三合一 → `34631906`：33 条原始划线去重 2 → **31 划线 + 4 想法**，章节「白鹿原/第四/五/十/十五/十六/十八章」（2026-09-05）。
- 《巨婴国》三合一 → 主本 `CB_CJ46…`（22 划线 1 想法，最全）并 `CB_ABS3…`（2）、`CB_BAv5…`（1），去重 1 → **24 划线 + 1 想法**，`outFile` 用干净名 `巨婴国 (武志红) (Z-Library).md`，三个旧文件全删（2026-09-06）。
- 《研究生论文写作与时间管理》二合一 → 壳用官方本 `3004247422`（它只有 3 条独立想法、0 划线），元数据基准用导入本 `CB_3TmB…`（47 划线、99%、读完时间），结果 **47 划线 + 3 条独立读书笔记**，保留官方文件名（2026-09-06）。
- 《社会学与生活》二合一 → `CB_9eI8…`（1 划线）并空副本 `CB_0tf9…`，结果 **1 划线**（2026-09-06）。
- 《中县干部》二合一 → 全本 `CB_Bq3A…`（冯军旗-中县干部（215页全），25 划线、章节完整，作基准与壳）并残本 `CB_FYdA…`（中县干部 (冯军旗) (Z-Library)，仅 1 划线且落在占位章 `_5`，与全本「第九章结语」同文）；新增的**跨章全局去重**去掉这 1 条重复并清空占位章，结果 **25 划线 + 0 想法**，保留全本文件名、删残本（2026-09-10）。
- 《国史大纲》二合一 → 九州官方繁体本 `23523074`（4 划线，元数据最全，作基准与壳）并个人导入简体本 `CB_ANp3…`（钱穆_国史大纲_简体中文版，1 划线，API 章节与繁体本一一对应、确认为繁简同书而非异译本），无重复，结果 **5 划线 + 0 想法**（简体序言 1 条 + 繁体第四十六章 4 条，均按原文用字保留），保留官方文件名、删简体导入本（2026-09-10）。
- 同期经 API 章节结构 + 划线重叠度判定**不予合并、各自保留**的同名书：《只是为了好玩》旧译本（CB_，43 章）vs《只是为了好玩：Linux之父林纳斯自传（修订版）》（3300197656，陈少芸译，51 章、65 划线 28 想法）——异译本；Learn to Read Greek Workbook（练习册）vs Workbook Textbook（课本，134/83 章、均 0 划线）——两本书；《社会学》3300117694（实为《社会学：批判的导论》）vs《社会学（第5版）》吉登斯 CB_ 本——两本书。

#### 以后再遇到新的重名书

1. 先按 §5.3 扫描磁盘（书名关键词 + frontmatter 的 bookId / progress / noteCount / reviewCount / 日期）确定每组的 survivor / 基准 / 待删；
2. 为 `order` 内所有 id 拉缓存（chapterinfo / bookmarklist / review(list/mine 翻全页) / getprogress）；
3. 在 `MERGE_GROUPS` **追加一组**（不要删历史组，它们可幂等复跑）；
4. 严格 `--dry-run` 核对每组合并计数与章节，再 `--write`；
5. 这属于改权威逻辑，交付说明里要写明改了哪几组、各并成几条、删了哪些文件。

### 5.5 换行符统一

- import 脚本写 **LF**；repair 脚本**已改为直接写 LF**（早期版本写 CRLF，历史上曾混用）。
- 仓库已加 `.gitattributes`（`* text=auto eol=lf`）兜底。
- 若仍遇到 CRLF 文件（外部拷贝、旧产物），遍历 `<ROOT>`（排除 site/.git/node_modules/.obsidian）把 md 的 `b'\r\n' → b'\n'` 覆写即可；**用 Python 处理，勿用 PowerShell 做中文文件名过滤**。

---

## 6. 工作流 C：站点构建、预览与浏览器测试

### 6.1 完整构建四步（在 `<SITE>` 下，按顺序，缺一不可）

```powershell
# 1) 把仓库各集合的图片同步进 public/media（先清空再复制，白名单见 §8）
node .\scripts\sync-assets.mjs
# 2) 类型 / 模板检查：必须 0 errors、0 warnings（hints 可接受）
.\node_modules\.bin\astro.cmd check
# 3) 静态构建
.\node_modules\.bin\astro.cmd build
# 4) 校验 dist 全部站内链接 / 资源可解析（有失效即非零退出）
node .\scripts\check-built-site.mjs
```

四步等价于 `pnpm build`。第 3 步会打印 `N page(s) built`，第 4 步打印 `已检查 N 个 HTML 页面`。**改了任何 md 或 src，都要重跑这四步。**

### 6.2 本地开发 / 预览

- 热更新开发：`.\node_modules\.bin\astro.cmd dev`（默认 4321；`启动网站.cmd` 也是干这个，但它内部调 pnpm，pnpm 坏了就直接用命令行）。
- 预览构建产物：`.\node_modules\.bin\astro.cmd preview`。
- 验证**纯静态产物**（最贴近线上）：

```powershell
Set-Location <SITE>\dist
python -m http.server 8123        # 后台起服，浏览器开 http://localhost:8123/
# 用完关掉该 python 进程
```

> `astro.config.mjs` 的 `base` 默认 `/`（可用环境变量 `SITE_BASE` 覆盖）；本地 dist 用根路径起服即可。

### 6.3 浏览器交互冒烟测试（用户说「检查并测试」时必做）

用浏览器自动化（`computer_use_tool`，`plane="bu"`，先读 browser-use-automation 技能）。**注意 `bu.js()` 执行的是表达式、不是箭头函数**（`bu.js("document.querySelectorAll('.x').length")` 可以，传 `() => …` 会得到空对象）。

书架模块最少要覆盖：

1. 首页：`#panel-bookshelf` 激活、书卡数量 = 当前书库本数、书背（分类）里**没有「未分类」**；
2. 点一个分类书背 → 只剩该类、可见卡数变化；点「全部」→ 恢复；
3. 搜索框（选择器 `#panel-bookshelf .module-search__input`，**不是** `.shelf-search input`）：输入书名 → 过滤；**填完 sleep 约 0.8s 等过滤生效再取结果**；清空 → 恢复；
4. 顶部切到「网络博主」→ `#panel-bloggers` 激活、有合集卡片；再切回；
5. 点一本书卡进详情页：H1 正确、`📌/💭` 数量与 frontmatter 一致、章节有序、`[...document.images].filter(i=>i.complete&&i.naturalWidth===0)` 坏图数为 0；
6. 阅读统计页 `/notes/微信读书阅读统计--<id>/`：总览/逐月表格渲染正常。

---

## 7. 工作流 D：新增 / 编辑内容（博主文章、专题、合集）

### 7.1 新增一篇博主文章

1. 在对应集合文件夹（如 `毒舌的南瓜\`）下新建 md，**文件名即默认标题**（frontmatter 不写 title 时）；
2. 最简 frontmatter（**`date` 必填**，写本文入库 / 收藏当天 `YYYY-MM-DD`，合集内靠它「最新在最前」，见 §9.4；微信读书导出的书不用写，走 lastReadDate 链）：

```markdown
---
title: 可选，不写则用文件名
date: 2026-09-10                     # 必填：入库日期，决定合集内排序（最新在最前）
cover:
  image: '文本附件/xxx-960.webp'   # 本地封面走 generate:covers 自动生成，见 §8
  actualRatio: '3:2'
draft: false                        # true 时线上不渲染（dev 可见）
sourceUrl: https://www.bilibili.com/…   # 或用 网址:/url: 字段；正文里「网址：http…」也能识别
---

正文 Markdown……
```

3. 需要子专题时，把 md 放进集合下的子文件夹（如 `渤海小吏\安史之乱\`），该层自动成为「专题（topic）」；`文本附件` 是保留目录名，不会被当成专题。
4. 跑封面生成（§8）→ 构建四步 → 它会出现在「网络博主」模块与对应集合页。

#### 把外部 txt 文稿（桌面/下载的视频文案、转写稿）批量入库

遇到一批 `博主名/标题.txt` 的外部文稿时，按下面的清洗规则转 md（可写一个一次性 Python 脚本批量做，**不要手工逐篇**）：

1. **目录 = 集合**：每个 `博主名` 文件夹对应仓库根的一个集合目录（已存在就并入，不存在就新建，默认 creator，无需登记）；文件名 = md 标题。
2. **去重复标题**：txt 首行若与文件名相同则删掉（标题由文件名承载，正文不要重复一遍）；首行不同（直接是正文）则保留。
3. **去 AI 处理痕迹**：删掉「以下是修订后的文本：」这类转写/润色残留行。
4. **图片占位**：转写稿里常见单独一行 `图片` + 下一行 `图：说明`，但没有真实图片文件——把两行合并成斜体图注 `*图：说明*`，删除裸 `图片` 行；若以后拿到真图再按 §8.3 插入。
5. **口播广告保留**：本库是「忠实档案」，已有文章普遍保留了赞助口播（如转转/优惠券段落），**不要擅自删广告**；只删明确的处理噪音。
6. **排版归一**：统一换行 LF、UTF-8；3 个以上连续换行压成段落间一个空行；去掉行尾空白；文件末尾留一个换行。**每篇 frontmatter 写 `date: 入库当天日期`**（批量同日入库可统一日期；想保留真实先后就按文稿发布 / 收藏日期分别写），否则合集内只能按标题兜底。
7. 转完逐篇抽查开头/结尾与图注，再走「生成封面 → 构建四步 → 浏览器冒烟」。
8. **读者评论就是正文的一部分（2026-09-22 用户钦定，仅限「网络博主」模块；覆盖此前任何特殊排版做法）**：桌面 txt 结尾若有读者评论（网名、地点、时间、点赞数、长评五项），它与 UP 主正文地位完全相同，**禁止**为其加分割线 `---`、`## 读者评论` 之类标题、bullet 列表、blockquote 引用块、斜体或任何特殊字体/栏目。按源 txt 原貌以**普通正文段落**接在正文之后：网名/地点/时间/点赞数在源文里各占一行的，用 Markdown 硬换行（行尾两个空格）保持分行、同属一个普通段落；长评按源文分段成普通段落。五项信息一字不丢、不脑补标签（源文是裸「141」就写 141，不擅自加「赞」字）。
9. **合理分段分行分条目（全站文本通用：微信读书划线/想法/笔记 + 网络博主文章）**：源文怎么分段就怎么分段，段落之间空一行；同一自然段不拆碎、不同段落不糊成一堵墙；并列条目各自成行。微信读书 md 由生成器保证每条 📌/💭 各占独立 blockquote 行、⏱/🕰 紧随其后（交付前全库扫描，0 同行堆叠、0 失配）；博主 txt 转 md 后必须做段落守恒核对（源段落与 md 对齐，允许把源文单换行分隔的小标题/自然段拆成独立段落，但不得删字、并段）。
10. **★段落怀疑线：140 个中文字（2026-09-22 用户强设定，交付前必须全库扫描）**：按**汉字**计数（不含标点、空白、西文），一个自然段（md 里空行分隔的文本块）汉字数 **>140** 即判为可疑的「一堵墙」，必须在自然边界拆成多段、拆后每段 ≤140。断点优先级：①句末标点 `。！？` → ②中文逗号 / 分号 / 顿号 `，；、` → ③英文 `, ;`（仅两侧都是西文）→ ④连接词前（并且/但是/因为/所以/如果/虽然/然后/另外/此外/同时/首先/其次/最后/也就是说/换句话说/其实/实际上/当然/不过/于是/因此/然而）；整段只有一个长句、无任何自然边界的原样保留并在交付说明中列出，**绝不在词语中间硬断**。拆分只增加段落边界（空行 / blockquote 内 `>` 空行），**不增删改任何一个字**（每个被拆段落必须「拆后直接拼接 === 拆前原文」）。适用范围（严格按模块）：
    - **网络博主模块：所有正文段落都查**（含读者评论、`微信读书统计` 合集篇的散文段落；表格行、代码块、frontmatter、标题行不拆）；
    - **我的书架模块：只查 💭 想法 / 评论与「章节点评与书评」的独立笔记**，长想法在**同一条目内**分段（blockquote 内用 `>` 空行续接，💭/🕰 标记与时间戳各只出现一次，条目数不变）；
    - **📌 划线原文永远不拆**（§2.0 一手性，多长都保留）；元数据 callout、⏱/🕰 时间戳行不拆。
    - 生成器（import / repair）渲染想法文本时必须内置同一套分段逻辑，避免下次同步回退；修复脚本见 §13.1。

### 7.2 新增一个集合（新 UP 主 / 新专题集 / 新书库）

- 直接新建根目录文件夹即可被自动识别，**默认类型是 creator**。
- 要改成专题集 / 书库，或加描述，编辑 `src/config/collections.ts` 的 `collectionOverrides`（`微信读书` 是 library，`于丹品读《论语》`、`哔哩名人演讲录` 是 series）。
- 排序：集合列表里 library 沉底，其余按中文拼音（`Intl.Collator zh-CN-u-co-pinyin`）。

### 7.3 哪些 md 不进站点

`content.config.ts` 的 glob 排除：`README.md`、`智能体操作手册.md`（本手册，运维文档不进站点）、`site/**`、`.github/.git/.obsidian/.makemd/.space/.trash/**`、`node_modules`、`AI 指令/**`、`**/文本附件/**/*.md`、`微信读书/微信读书Gallery.md`；以及任何 `draft: true`（仅线上排除）。新增根目录运维类 md 时，记得同步在此加排除规则。

### 7.4 frontmatter 全部受支持字段（schema）

通用：`title/description/summary/cover/draft/date/网址/url/sourceUrl`（`date` = 外部博主文章入库日期，合集内最新在最前的排序键，见 §9.4）；
微信读书专用：`doc_type/bookId/reviewCount/noteCount/bookmarkCount/author/translator/category/publisher/publishTime/rating/progress/readingTime/readingDate/finishedDate/isbn/lastReadDate/aliases`。未知字段用 catchall 放行但不保证生效。**不要发明语义不明的新字段，先看 catalog.ts 怎么消费。**

### 7.5 编辑微信读书 md 的注意事项

- 书摘 md 是脚本生成物，**手工改动会在下次同步/修复时被覆盖**。要长期生效的改动应改生成脚本（import/repair）或源数据。
- 固定结构标记：`# 元数据`、`# 高亮划线`、`# 章节点评与书评`；划线 `> 📌`、划线时间 `> ⏱`、想法 `> 💭`、想法时间 `> 🕰`。修复脚本依赖这些锚点解析「外壳」，别随意改名。

---

## 8. 工作流 E：封面与图片（WebP 硬性规范）

> 用户硬性偏好：**网站上的图片必须使用体积小的 WebP 格式。** 任何新增图片优先 WebP，不引入 png/jpg 大图到站点。

### 8.1 资源同步 sync-assets

- `node scripts/sync-assets.mjs` 会**先清空** `public/media`，再把仓库里（排除 `.git/.github/.makemd/.obsidian/.space/.trash/AI 指令/node_modules/site` 与点号目录）的图片复制进去，路径保持 `<集合>/文本附件/xxx`，并写 `_manifest.json`。
- 允许扩展名白名单：`.gif .jpeg .jpg .png .svg .webp`。**新图一律用 .webp**；位图别用 svg 冒充。

### 8.2 自动补封面与完整性检查 generate-missing-covers

```powershell
node .\scripts\generate-missing-covers.mjs                  # 正式生成：补缺 + 修复失效引用
node .\scripts\generate-missing-covers.mjs --check          # 封面完整性检查：只报告，不写入
node .\scripts\generate-missing-covers.mjs --preview        # 只出风格预览，不改 md
node .\scripts\generate-missing-covers.mjs --regenerate-all # 全量重生成现有 generated-cover（不回写 frontmatter）
```

- **默认模式**处理两类已发布 md（均非 draft）：① frontmatter **没有 `cover:` 块**；② `cover:` 块**引用的本地图片文件不存在**（远程 http/https 封面视为有效）。**幂等、不覆盖「封面文件真实存在」的文章**。
- 以**纯 SVG 母版**（暖白纸底、细双线框、衬线标题、极淡线性图书馆母题，8 类配色区分，见脚本 `palettes`）按「标题+集合+路径哈希」**确定性**生成图书馆风格封面，输出到该 md 同级 `文本附件/`：
  - `generated-cover-<hash12>-960.webp`（960×640，q72）
  - `generated-cover-<hash12>-480.webp`（480×320，q68，作为 srcset 小图）
- 同时把 **`cover:` 块（键必须带冒号）** 写回该 md 的 frontmatter，并登记到 `assets/cover-system/generated-cover-manifest.json`（默认模式整体覆写清单，只含本次处理记录；该清单是纯登记用途，无其他消费者）。
- catalog 识别到 `-960.webp` 且存在 `-480.webp` 时会自动产出 `480w/960w` srcset；`_thumb.webp` 若存在会作为缩略图。
- **`--check` 是交付前必跑的封面完整性检查**：逐篇报告「缺封面 / 封面引用失效」，有需处理项时 exit code = 1；输出 **`全部已发布 Markdown 封面完整（无缺封面、无失效引用）`** 且 exit 0 才算通过。
- **坑（2026-09-13 实际踩到，已修复）**：frontmatter 里 `cover:` 引用本地文件但文件不存在时，站点不会显示封面（catalog 会丢弃失效引用），而旧版脚本「只要有 cover 块就跳过、不校验文件存在性」导致漏修。现在默认模式会校验存在性、重新生成并回写正确引用。同一批还发现 `4b149b8` 重构把 coverBlock 误写成裸 `cover`（无冒号），catalog 无法解析、新封面不显示——已修复为 `cover:`，且 `replaceOrInsertCover` 会兼容原地替换历史遗留的裸 `cover` 块（避免重复堆积）。
- **坑：脚本会遍历仓库里所有 md（不止站点内容）**。运维类根目录文档（`README.md`、`智能体操作手册.md`、`微信读书/微信读书Gallery.md`）已在脚本里显式 `continue` 跳过；**以后新增任何不应进站点的根目录 md，必须同时在两处加排除**：`content.config.ts` 的 glob 与本脚本的跳过判断，并从 manifest 里移除误登记记录、删掉误生成的根 `文本附件/`。

### 8.3 手动放封面 / 插图

- 封面路径相对 md 所在目录解析（也兼容相对仓库根）；本地图必须真实存在，否则 catalog 会丢弃该封面。
- 远程封面（微信读书图床 qlogo.cn 等）支持 http/https，展示层会**自动升级为 https** 避免混合内容拦截。
- 正文图片走 remark-obsidian 插件处理 Obsidian 语法（`![[…]]`、`![…](…)`、wiki 链接）；改链接规则去 `src/plugins/remark-obsidian.mjs`。

---

## 9. 「我的书架」模块原理与改造点

### 9.1 数据管线

`BookshelfModule.astro` → `getBookshelf()`（`src/lib/bookshelf.ts`）→ `getCatalog()`（`catalog.ts`）。

- **入选条件**：`collectionName === '微信读书'` 且 `kind === 'book'`（即 `doc_type: weread-highlights-reviews`）且非 draft。统计页（stats）、Gallery 不进网格。
- **分类**：取 frontmatter `category` 的**父类**（`精品小说-社会小说` → `精品小说`，按第一个 `-` 切）；缺 category 时走 `FALLBACK_RULES` 关键词兜底；都不中才是 `未分类`（正常不应出现）。
- **分类书背顺序**：`BOOKSHELF_CATEGORY_ORDER`（文学→社会文化→历史→…→生活百科，未分类固定最后），只输出有书的分类。
- **网格排序**：`compareByRecent`，排序键 `lastReadDate → finishedDate → readingDate`，**从新到旧**（YYYY-MM-DD 直接字符串比较），同日期按标题中文排序；三个日期都空（加入书架没读过、0 笔记）的沉底。这是设计预期，不是 Bug。
- 每个分类内部也按同一规则从新到旧。

### 9.2 常见改造

- **某本无 category 的书显示成「未分类」**：在 `bookshelf.ts` 的 `FALLBACK_RULES` 对应分类正则里**追加书名关键词**（自上而下先命中先生效，注意顺序，例如「人民文学」要在通用报刊规则之前）。这是**展示层 sanctioned 扩展点，只改这里、不回写 md**。2026-09-05 就为「人民日报」在政治军事正则补了关键词。
- **调整分类先后**：改 `BOOKSHELF_CATEGORY_ORDER`。
- **改排序口径**（如想按读完优先）：改 `bookSortKey/compareByRecent`，并同步检查详情页/集合页是否共用。
- **改卡片样式 / 书背 / 搜索框 / 3D 场景**：`components/modules/BookshelfModule.astro`（3D 是 `public/3d/residence-smith.html#cobalt` 的 iframe）与 `src/styles/modules.css`。

### 9.3 笔记编排规则（详情页）

- 章顺序 = 章节 `chapterIdx`（导入器）或修复脚本重建顺序；章内 = 划线 range 起点（**书中位置，不是划线时间**，所以时间戳可以不连续，属正常）；
- 同一划线下的想法按时间升序；独立想法/书评在「章节点评与书评」按时间升序；
- 数量一致性：`noteCount == 正文 📌 数`；`reviewCount == 正文 💭 数 + 「章节点评与书评」下 ## 条目数`（独立条目**没有** 💭 标记，校验时别漏加，否则会误报）。

### 9.4 全站排序铁律：容器内文本「最新在最前」（用户排放习惯）

用户习惯（两个模块口径一致，任何改动不许违反）：

- **「我的书架」**：不管哪个书架分类，分类内部的图书按最新阅读时间从新到旧（§9.1 的 `bookshelf.ts/compareByRecent`，键 `lastReadDate → finishedDate → readingDate`）；
- **「网络博主」**：不管哪个创作者 / 专题合集，合集内部的文章（以及专题页）同样**最新在最前**——由 `catalog.ts` 的 `noteRecentKey(note)` 统一取键：**外部博主文章用 frontmatter `date`（入库日期）；微信读书导出内容用 `lastReadDate → finishedDate → readingDate`**；再由 `compareNotes`（时效降序、无日期沉底、同日标题拼音兜底）作用于全局 notes、每个 `collection.notes`、每个 `topic.notes`；`BloggersModule.astro` 的搜索「相关文本」索引也用同一时效键降序。
  - 外部 UP 主 / 视频文案**没有微信读书日期**，所以新增时必须按 §7.1 在 frontmatter 写 `date`；2026-09-10 已为存量 205 篇无日期外部文章按 **git 首次入库日期**回填（其中 199 篇为 2026-09-02 批量导入、6 篇为 2026-09-06），同日文章按标题拼音兜底，今后每篇新文章写当天日期即自动置顶。
- **不动的容器导航**：书架分类书背顺序（`BOOKSHELF_CATEGORY_ORDER`）、网络博主合集卡片的拼音首字母分组、合集 / 专题自身顺序，保持固定；变的只是容器里面的文本。
- 排序只发生在展示层，**绝不回写 md、绝不改动微信读书一手内容**（与 §2.0 一致）。

---

## 10. 工作流 F：部署到 GitHub Pages

- 工作流 `.github/workflows/pages.yml`：push 到 `main`（或手动 workflow_dispatch）触发；`withastro/action@v6`，`path: ./site`，`node-version: 24`；随后 `deploy-pages@v5`。
- 环境变量：`SITE_URL=https://20260718.xyz`、`SITE_BASE=/`、`ASTRO_TELEMETRY_DISABLED=1`。**若改部署到 project pages（路径带仓库名），把 `SITE_BASE` 设成 `/<仓库名>/`，本地构建也要带同样变量**，否则资源 404。
- 首次启用：仓库 `Settings → Pages → Build and deployment → Source = GitHub Actions`。
- 发布前本地四步必须全绿；线上地址以 README / 工作流为准。
- **提交后自觉推送（用户钦定）**：本地 `git commit` 完成、§11 验收全过后，**直接 `git push origin main`，不用再问用户**；推送即触发 Pages 部署。推送后回报远程结果（如 `f531958..xxxx main -> main`）。仅当 push 失败（冲突 / 认证 / 网络）时才停下报告。

---

## 11. 交付前验收清单（逐项打勾）

### 11.1 数据层（Python，勿用 PowerShell 中文过滤）

- [ ] `微信读书\` md 数量 = 书籍数 + 阅读统计 1 + Gallery 1；
- [ ] **每本**书 `noteCount == 正文 📌 数`，0 不符；
- [ ] **每本**书 `reviewCount == 💭 数 + 「章节点评与书评」## 条目数`，0 不符；
- [ ] **完整性 / 一手性（§2.0）**：同步 / 新增 / 整合前后，划线与想法总数守恒（合并只减真正重复项，独特内容 0 丢失）；无手工改写原文、无擅自删除；不同译本/版本仍各自独立；
- [ ] 数字章节无倒序（第 2 章不应排在第 10 章后；注意中文数字要按数值比较）；
- [ ] 重名书按用户要求处理（如白鹿原只剩 1 个文件、书架只剩 1 张卡）；
- [ ] 有笔记的书三个日期不全空（全空疑似导入异常）；
- [ ] 全库 md 统一 LF（配合 .gitattributes）；
- [ ] **§7.1 第 10 条 140 字怀疑线全库扫描为 0**：网络博主模块所有正文段落、书架模块全部 💭 想法/评论与独立笔记，均无 >140 汉字自然段（📌 划线原文不扫、不拆）；被拆段落逐字守恒、💭/🕰 条目数不变；
- [ ] 临时目录（`.weread-export`、`…\obsidian-weread-plugin-main`）已删除。

### 11.2 展示层兜底

- [ ] 用 `FALLBACK_RULES` 全量模拟一遍分类，**兜底后仍「未分类」= 0**；没有就补关键词；
- [ ] 书架网格排序 0 逆序（按 §9.1 排序键从新到旧）。

### 11.3 构建层

- [ ] **封面完整性检查**：`node .\scripts\generate-missing-covers.mjs --check` 输出「全部已发布 Markdown 封面完整」且 exit 0（有缺封面 / 失效引用必须先补全，见 §8.2）；
- [ ] sync-assets 成功（打印同步图片数）；
- [ ] `astro check`：0 error、0 warning；
- [ ] `astro build` 成功，记录页面数；
- [ ] `check-built-site`：站内链接与资源全部可解析；
- [ ] dist 里书卡数量、目标书（如白鹿原）唯一性、分类书背无「未分类」与数据层一致。

### 11.4 浏览器冒烟（§6.3）

- [ ] 首页双模块、分类筛选、搜索（记得等过滤）、模块切换；
- [ ] 抽查 1–2 本详情页：📌/💭 数、章节顺序、坏图 = 0；
- [ ] 阅读统计页渲染正常。

### 11.5 交付说明要写清

改了哪些文件 / 数据关键数字 / 构建页面数 / 验证方式与结果 / 尚未处理项（例如其他重名书保持原样要明确告知）。

### 11.6 提交与推送

- [ ] `git add -A` 后核对暂存清单（无临时目录、无 node_modules/dist/public-media）；
- [ ] 按 §13 约定写提交信息并 `git commit`；
- [ ] **直接 `git push origin main`（用户已授权自觉推送，无需询问）**，确认远程分支推进成功；push 失败才停下报告。

---

## 12. 常见故障与踩坑手册

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| 网关返回 **499** / 接口像没生效 | `api_name` 漏了前导 `/`（写成 `shelf/sync`） | 统一写 `/shelf/sync` 形式 |
| **errcode -2007** | PowerShell `curl` 给 JSON 加了 BOM | 改用 Python urllib（§4.2） |
| 分页失效、永远第一页 | 业务参数被包进 `params`，或没平铺 `lastSort/synckey` | 参数与 api_name/skill_version 同层平铺；游标逐页取到无下一页 |
| 回包出现 `upgrade_info` | skill 版本过期 | 立即暂停，按 message 升级版本号后重来，不得忽略 |
| 个别接口连接重置 | 网络抖动 | 指数退避重试 3 次；仍失败则只补该本，直到 manifest failureCount=0 |
| 导入器报「仍有 N 个失败项」 | export-manifest 的 failureCount≠0 | 补抓失败书/接口后重新导出，再导入 |
| `pnpm` 命令报错 / shim 缺 cjs | 本机 pnpm 损坏 | 全部改用 `node` 与 `node_modules\.bin\astro.cmd`（§3.1） |
| PowerShell 中文文件筛选返回空 | PS 对中文通配/Where-Object 不可靠 | 用 Python os/re 处理 |
| `python -c` 中文/引号报错 | PS 解析破坏内联代码 | 写成 .py，`python -X utf8 文件.py` |
| repair 报找不到缓存 / noCache 很多 | 缓存路径错（必须在仓库**同级**的 obsidian-weread-plugin-main\weread-export-tool\.cache）或结构键名不符 | 按 §5.2 建目录与结构；缓存只放目标书 |
| repair 后想法数还是不对 | 缓存里 review 没翻全页，或 review 包了一层 `.review` | review/list/mine 用 synckey 取全；脚本兼容 item.review ?? item |
| 修复后文件变 CRLF | 极早期 repair 版本写 CRLF（现版本已直接写 LF） | 落盘后转 LF（§5.5）兜底 |
| 书显示「未分类」 | md 无 category 且兜底规则没覆盖 | 在 bookshelf.ts FALLBACK_RULES 加关键词（展示层，不改 md） |
| 文章没显示封面，但 `文本附件/` 里明明有封面文件 | frontmatter `cover:` 引用的文件名与磁盘实际文件不一致（如旧式手编名 `generated-cover-xxx-960.webp` vs 实际哈希名 `generated-cover-<hash12>-960.webp`）；或封面块被写成裸 `cover`（无冒号） | 跑 `generate-missing-covers.mjs` 默认模式（校验文件存在性并回写正确引用）；检查 frontmatter 是否 `cover:` 带冒号 |
| 新生成的封面在站点上不显示 | coverBlock 曾误写为裸 `cover`（无冒号），catalog 无法解析 | 已修复为 `cover:`；`replaceOrInsertCover` 兼容原地替换历史裸块，重跑生成即可 |
| 封面完整性检查 exit 1 | 存在缺封面或失效引用的 md | 跑默认模式补全后重新 `--check`（§8.2） |
| 本地起服图片/CSS 404 | SITE_BASE 与起服路径不一致 | 本地用默认 base=/ 从 dist 根起服；子路径部署同步设 SITE_BASE |
| 4321 端口被占 | 旧 dev 没关 | `启动网站.cmd` 自带清理；手动：找占用 PID 后结束进程 |
| 浏览器 `bu.js(箭头函数)` 返回 `{}` | bu.js 执行表达式而非函数 | 传纯表达式字符串（§6.3） |
| 搜索填了词但结果没变 | 过滤有微小延迟 / 选错选择器 | 用 `#panel-bookshelf .module-search__input`，填后 sleep 0.8s |
| 构建通过但页面没更新 | 改了 md 没重建，或看的是旧 dist/缓存 | 重跑四步；静态服强刷 |
| astro check 刷一大段 minified 输出 | 依赖包源码被打进诊断流，结尾 Result 才是结论 | 只看最后的 `0 errors / 0 warnings` |
| 读书笔记色块在列表处中断、列表后正文无色块 | `transformWereadNotes` 只收连续段落，遇 list 即停且要求紧随时间戳 | 按笔记标题整组分组，段落+列表统一挂 `note-card__body`（列表加 `--list`），首尾块拼圆角（§18） |
| 统计总览「日均阅读时长」显示 0 | 总览 API 无 `dayAverageReadTime` 字段，生成器直接取值 | 总览/逐月日均一律按「时长 ÷ 阅读天数」现算，不信 API 字段（§18） |
| 历年表整体早标一年、每日表日期早一天 | 年/日键是北京零点，`getUTCFullYear()`/`toISOString()` 按 UTC 渲染 | 日期统一 `Intl.DateTimeFormat({timeZone:'Asia/Shanghai'})`；年键显式 `+8h` 后取 UTC 年（§18） |
| astro build 后台任务挂死、所有命令/文件通道超时数小时 | `2>&1 \| Select-Object -Last N` 后台管道死锁，占住工具调度 | 构建输出一律重定向到文件（`*> build.log`）再读日志；禁用 Select-Object 管道（§18） |

---

### 12.7 重导后重名副本复活 + 内容缩减（2026-09-22 返工）

- **症状**：全量重跑 weread 同步后，§15 已合并的 6 组重名书又分裂成多个 CB_ 副本；8 本书相对上一版丢失划线/想法。
- **根因**：(a) import 脚本从 API 拉到的 CB_ 副本直接落盘，没有重跑 repair 合并；(b) reviews 端点 HTTP 499 时数据残缺，import 仍用残缺数据整体覆盖旧 md。
- **修复**：
  1. 重导后**必须**重跑 `repair-weread-notes.mjs --write`（MERGE_GROUPS 覆盖 6 组：白鹿原/巨婴国/研究生/社会学与生活/中县干部/国史大纲），删副本、合并到幸存者。
  2. import 脚本加 fail-closed：新渲染 📌/💭 计数少于旧文件时跳过该本，保留旧内容。
  3. 已确认丢失的内容用 `git show <old>:<path>` 取旧版并集补回。
  4. **副本文件名可能与书名完全不同**（如 bookId=CB_6fC4… 的白鹿原副本文件名是「下载白鹿原201209.md」），按书名/文件名找副本必然漏网。repair 已加**磁盘兜底**：每组合并写出后扫库目录，凡 frontmatter `bookId` 命中 removeIds 且不是本组 outFile 的 md 一律删除。验收也必须按 frontmatter bookId 全库扫描，不能只按文件名/书名聚类。

---

## 13. 关键文件地图

### 13.1 `site/scripts/` 运维脚本

| 脚本 | 职责 | 关键约定 |
| --- | --- | --- |
| `import-weread-export.mjs` | 导出目录 → 书摘 md + 阅读统计 | 参数=导出目录；manifest failureCount 必须 0；按 bookId 更新；写 LF |
| `repair-weread-notes.mjs` | 用 API 缓存重建笔记、模糊挂载想法、重名书多副本合并（章内 + 跨章两层去重） | 缓存见 §5.2；`--dry-run/--write/--only=/--dump`；写 LF |
| `seg140.mjs` | **140 字怀疑线唯一分段实现**（§7.1 第 10 条）：导出 `hanLen/splitLong/thoughtLines/noteBody`，import 与 repair 共用；💭 想法多段 blockquote、独立笔记空行分段、📌 不拆 | 改分段规则只准改这一个文件；`splitLong` 内置剥标签守恒断言，与 Cahier `_tools/seg140.py` 规则同源 |
| `sync-assets.mjs` | 仓库图片 → public/media（先清空） | 扩展名白名单、跳过目录见 §8.1 |
| `generate-missing-covers.mjs` | 补缺封面 + 修复失效封面引用，写回 frontmatter；`--check` 只查不写 | 幂等不覆盖；`--check`/`--preview`/`--regenerate-all`；cover 键必须 `cover:` |
| `generate-favicon.mjs` | 生成站点 favicon | 按需运行 |
| `check-built-site.mjs` | 校验 dist 站内 href/src 全部可落地 | 有失效即非零退出 |

### 13.2 `site/src/` 核心源码

| 文件 | 职责 | 改动场景 |
| --- | --- | --- |
| `content.config.ts` | md 扫描范围 + frontmatter schema | 新增受支持字段 / 调整排除范围 |
| `config/collections.ts` | 集合类型 creator/series/library 与描述 | 新增特殊集合 |
| `lib/catalog.ts` | 全库 → Note/Collection/Topic 目录模型、封面/摘要/外链解析 | 改内容如何被识别 |
| `lib/bookshelf.ts` | 书架入选、分类兜底、排序 | 加兜底分类、调排序/分类顺序（§9） |
| `lib/url.ts` | withBase、mediaUrl、stableId（页面稳定 ID） | 改 URL 规则 |
| `lib/pinyin.ts` | 中文拼音排序辅助 | — |
| `components/modules/BookshelfModule.astro` | 书架 UI（书背/搜索/网格/3D）+ 前端过滤 | 改书架交互与样式 |
| `components/modules/BloggersModule.astro` | 博主模块 UI | 改博主侧 |
| `pages/index.astro` | 首页双模块 + 切换脚本（#bookshelf/#bloggers） | 改首页结构 |
| `pages/notes/[id].astro` | 文章 / 书摘详情页 | 改详情渲染 |
| `pages/collections/[id].astro`、`pages/topics/[id].astro` | 集合页 / 专题页 | 改列表页 |
| `layouts/BaseLayout.astro`、`styles/*.css`、`plugins/remark-obsidian.mjs` | 布局、全局样式、Obsidian 语法解析 | 改皮肤 / Markdown 语法 |

### 13.3 书摘 md 模板（微信读书）

```markdown
---
doc_type: weread-highlights-reviews
title: "书名"
bookId: "123456"
reviewCount: 4
noteCount: 31
bookmarkCount: 0
author: "作者"
category: "精品小说-社会小说"
progress: "38%"
readingTime: "1小时7分钟"
readingDate: 2025-09-12
lastReadDate: 2026-09-05
cover: "https://…(可选)"
---

# 元数据
> [!abstract] 书名
> - 书名：…
（作者/简介/进度/时长/最后阅读/微信读书链接）

# 高亮划线
## 章节名
> 📌 划线原文（原文多长都不拆）
> ⏱ 2025-09-12 16:30:00
> 💭 我对这条划线的想法第一段（>140 汉字时在同一条目内分段）
>
> 想法第二段（blockquote 内裸 > 空行续接，💭/🕰 各只出现一次）
> 🕰 2025-09-12 16:31:00

# 章节点评与书评      （没有独立想法时整节不存在）
## 读书笔记
独立笔记第一段（源文单换行 / 3+ 空格即段落边界，段间空一行，长段按 140 拆）

独立笔记第二段

> 记录于 2025-09-12 16:31:00
```

---

## 14. 本次（2026-09-16）变更记录：博主新增、微信读书全量同步、封面补全、标点纠察、同源设计走查

### 14.1 网络博主新增 6 篇

- 大猿取经 4 篇（《PDF转成word？做不到》《PDF转word一定要做，怎么办？》《人设最可怕的地方，是你演久了以后，开始替它看门》《只有形式，没有本质》）；啊粥粥啊粥 2 篇（《以众生的名义，如来佛祖如何布局「西天取经」？》《大战小雷音！…弥勒佛为什么不开心？》）。
- **口播广告原样保留**：大猿取经篇内的「青丝几鹤洗发水」口播是作者原文的一部分，按 §2.0 忠于原文铁律，不删不改。
- 直角引号「」在**博主正文叙事**中改为中文弯引号「“”」（与全站自撰正文统一）；注意这与手册 / README 里**刻意用「」标记 UI 元素 / 术语**是两回事，后者保留。

### 14.2 微信读书全量同步结果（数字留痕）

- 全量抓取 236 本在架电子书（197 本有笔记），接口 `failureCount=0`，阅读统计覆盖 25 个月；导入结果「更新 218、新增 18」。
- 书架网格最终 **250 张卡 = 236 本云端在架 + 14 本本地 / 历史书**（统计页、Gallery 不进网格），浏览器实测 18 个分类书背、**无「未分类」**、坏图 0。
- **14 个「md 有、本次云端书架 236 无」的文件全部保留、禁止删**：6 本 `CB_` Z-Library 本地导入、3 篇 `MP_WXS_` 公众号、5 本数字 bookId 云端历史书（《中国文化史导论》《水浒传（人民文学版）》《金瓶梅》《研究生论文写作》等）。它们含真实划线笔记，API 书架不返回不代表用户要删；§11.1 的「md 数 = 书籍数 + 2」应理解为**网站实际收录数**（含这些本地 / 历史书），不是「本次 API 返回数」。
- **重名副本本次不合并**（合并是 §5 专项、需缓存 JSON，避免临近交付大改正文）：巨婴国 3 份、白鹿原 2 份、社会学与生活 2 份、中县干部 2 份、曼昆 2 份、研究生论文写作 2 份。后续要合并走 `repair-weread-notes.mjs`，交付时已向用户列明。

### 14.3 reviewCount 已知遗留（划线 100% 完整，仅少量想法挂载差异）

- `noteCount == 正文 📌 数`：**251 本全部相符，0 不符**，划线一条不丢。
- `reviewCount` 有 15 本 frontmatter 数大于正文承载数，分两类：①13 本各差 1–3 条（API 缓存 `reviews.reviews` 齐全，是导入时想法没挂上划线、也没收进「章节点评与书评」）；②《台北人》（166）、《焦虑的一代》（135）评论超 100，全量同步时 reviews 单页只取首页 100 被截断。
- **找回路径（后续专项）**：①类用 §5 的 `repair-weread-notes.mjs`，但它固定从 `..\obsidian-weread-plugin-main\weread-export-tool\.cache\<bookId>.json` 读 `{chapterResp,highlightResp,reviewResp,progress}` 结构，需先把 API 缓存适配成该结构，先 `--dry-run --only=书名` 对比 diff，确认只增不丢再 `--write`；②类需重新调 review/list 翻页拉全（注意单页 100 上限，用游标翻到无下一页）。本次缓存 `.weread-export` 按 §4.6 已删，重拉即可。

### 14.4 封面补全与构建四步结果

- `generate-missing-covers` 补 7 篇（6 篇新博主 + 《微信读书阅读统计》），共 14 张 WebP（960+480，合计 0.09 MiB）；18 本新书带微信读书远程 qlogo 封面视为有效，无需生成。复跑 `--check` 输出「全部已发布 Markdown 封面完整」、exit 0。浏览器实测合集 9 篇全部加载 generated-cover WebP、**冷灰 fallback 消失**、坏图 0。
- 构建四步（`<SITE>` 下，pnpm 已坏，astro 一律 `node node_modules/astro/bin/astro.mjs` 直跑）：sync-assets **421 图 4.85 MiB** → astro check **0 error 0 warning 23 hints** → astro build **564 页**（49.15s）→ check-built-site **565 个 HTML 站内链接资源全可解析**。

### 14.5 中文标点纠察方法论（与 Cahier 十五节同源）

- 博主文案、AI 指令、根 md、本手册等**自撰内容**用**段落级配对状态机**规范半角直引号：空行分段、段内奇偶交替、纯英文 / 无 CJK 段保留半角，保护 frontmatter / 围栏代码 / 行内代码 / HTML 属性 / URL。本次 12 个自撰 md 改 340 处直双引号 + 13 对直角引号 + 6 处错字 / 撇号（如「入场券」、People’s、I’ve / It’s、‘大以色列’）。
- **不改白名单**：微信读书导入的 236 本原著 / 译本（含原书「」、口语重复、ASR 口吃）、frontmatter、HTML 属性、代码公式、GB7714 参考文献、英法文 apostrophe / 省音撇号，一律保持原样。

### 14.6 同源设计 diff 结论（苹果风二次走查，Restraint）

- 本站书架书脊 `.shelf-spine` 的 `--cloth` 是 BookshelfModule 里 16 色低饱和莫兰迪布面色（墨绿 / 砖红 / 赭石 / 灰蓝…），属「布面教材」拟物隐喻，**刻意不鲜艳，禁止「提亮」**；hover translateY(-5px)/active(-9px) 配 --ease-spring 是有意手感。缺封面卡片冷灰底只在缺封面时出现，补封面即消失。
- 本站的 7 套明亮主题 + `.theme-cloud` 小云切换器，与 Cahier 的明暗双模 + View Transitions 圆形破口动画是**两条有意的产品路线**，同源项目之间**只 diff、不互相覆盖**。三站设计系统均已成熟，本次不做样式大改。

### 14.7 环境教训

- Windows PowerShell 内联 Python 含中文 / 弯引号 / 反引号必报解析错：写 `.py` 再 `python -X utf8 file.py`；`Get-Content` 读 UTF-8 无 BOM 显示乱码是终端问题，判编码用 Python，别误判文件损坏。
- `node node_modules/astro/astro.js` 会 MODULE_NOT_FOUND，正确入口是 `node_modules/astro/bin/astro.mjs`。

---

## 15. 本次（2026-09-19）变更记录：微信读书全量同步、21 本想法找回、6 组重名合并

### 15.1 全量同步结果（数字留痕）

- 全量重抓：`/shelf/sync` 259 条目（258 电子书 + 1 公众号入口，0 专辑），`/user/notebooks` 游标翻页 224 本有笔记，并集 **273 本**；12 本章节目录 HTTP 499 为瞬时错误，串行补抓后 manifest `failureCount=0 / warningCount=0`。
- 导入结果「更新 273、新增 0」（当天早些时候已导过一次，0 新增相对该次而言）；相对上一次提交，站点书卡 **250 → 267**（25 个新文件、8 个合并副本删除）。
- 阅读统计 25 个月（2024-09 ~ 2026-09）：累计 346 754 秒 = **96 小时 19 分 / 159 天 / 读过 148 本 / 读完 14 本 / 笔记 7 489 条**。
- 浏览器实测：书架面板 **267 张卡、18 个分类书背、无「未分类」、坏图 0**；分类筛选、搜索（「白鹿原」唯一命中）、双模块切换、博主合集/文章详情、统计页全部通过。

### 15.2 §14.3 reviewCount 遗留本次清零：21 本想法找回

- 关键修正：网关 `/review/list/mine` 用 **`count=200` 可单页取全**（台北人 166、焦虑一代 135 均不再截断；取后必须核对 `totalCount` 防截断），§14.3 说的「单页 100 上限」对当前网关已不成立。
- 21 本想法挂载丢失全部用 `repair-weread-notes.mjs` 重建，典型：台北人 179 划线 / 166 想法（163 内联 + 3 条独立「章节点评与书评」）、小径分岔的花园 143/45、未来简史 364/37、焦虑一代 234/135。
- 校验方法（交付前必做）：划线集合与想法（内联 💭 + 独立节 `## 读书笔记` 条目，注意是 **h2 不是 h3**）逐条与云端 data.json 比对集合守恒；章节序按 chapterIdx（多卷本章回编号按卷重启是误报，以 chapterIdx 为准）。终检 267 本 ALL PASS。

### 15.3 6 组重名副本合并（§14.2 遗留本次完成）

- 白鹿原 3→1（`白鹿原.md`，30 划线 / 4 想法；33→30 为 2 条包含 + 1 条全等）、巨婴国 3→1（23/1）、研究生论文写作与时间管理 2→1（49/3）、社会学与生活 2→1（1）、中县干部 2→1（25，跨章 1 条全等）、国史大纲 2→1（`国史大纲（全2册）.md`，5）；共删 8 个副本，去重条目逐条人工核对均为真重复。
- **曼昆两本判定为不同作品，禁止合并**：27049318 是圣才《复习全书》，`CB_9O6…` 是 Z-Library 曼昆《经济学原理》教材。
- 云端已不返回的本地公众号书《维克多英语》《账号已迁移》按 §14.2 原则保留。

### 15.4 新坑与脚本/规则改动

- **全量重导会按 bookId 重建 `CB_` 后缀副本**，冲掉上次合并结果。已给 `repair-weread-notes.mjs` 的 MERGE_GROUPS 增加 `outFile` + `removeIds`（白鹿原组、研究生论文组），使合并幸存者落到干净幂等文件名；脚本逻辑保证 victim 路径 == outPath 时跳过删除。后续新增合并组必须照此配置，否则每次同步都要手工清副本。
- 书架兜底分类（`src/lib/bookshelf.ts`，仅展示层）：新增「中国新闻社」→ 社会文化（原规则只有中国新闻周刊 / 中国新闻网）。**每次同步后必须用兜底规则全量模拟一遍分类，0 未分类才允许构建。**
- **统计页封面会被导入器整文件覆盖**：每次同步后 `generate-missing-covers --check` 必报统计页缺封面，跑一次默认模式补回即可（本次 2 张 WebP / 0.01 MiB）。
- 构建四步结果：sync-assets **435 图 / 4.95 MiB** → astro check **0 error 0 warning 23 hints** → astro build **590 页** → check-built-site **591 个 HTML 全可解析**。

### 15.5 环境教训补充

- 本机 PATH 默认 `node` 可能是 Doubao 沙箱自带 **v22**（不满足 engines `>=24`）：若 `node --version` 不是 24，所有 site 命令显式用 `& "C:\Program Files\nodejs\node.exe"`（v24.15.0），pnpm 已坏一律 node 直跑 `node_modules/astro/bin/astro.mjs`。
- 临时目录（`.weread-export` 整棵、同级 `obsidian-weread-plugin-main` 缓存树）交付前删除；PowerShell 删目录偶报「另一进程使用」（索引器 / 杀软瞬时句柄），先逐个子项删、再 `cmd /c rmdir /S /Q` 清壳即可。
- **本地预览服务器（`python -m http.server`）收尾前若用户仍在浏览器里看页面，不要关**：封面图是 `loading="lazy"`，服务器停掉后再滚动，未加载的图会全部显示成坏图（内容本身没坏，重启服务器刷新即恢复）；交付前的坏图核查必须滚动到底触发全部懒加载。

### 15.6 网络博主新增 3 篇（2026-09-19 入库）

- 新合集 2 个：**燕三嘤嘤嘤** 1 篇（《印度的实力已经跟不上他的野心了》）、**酒痴东梦** 2 篇（《北边拳头大，南边大拳头！日本军部为何选择进攻东南亚？》《瘟疫爆发了也要泡澡？罗马帝国如何开进下水道1【意大利为何这么菜01】》），均为桌面 `网络博主\` 文件夹的 txt 转写稿，按 §7.1 批量转 md（UTF-8 / LF / date=入库当天），原文弯引号已成对、无图片占位与 AI 痕迹。
- 口播广告原样保留：燕三篇尾「拼多多 87723 百亿补贴」、酒痴日本篇开头「叮答鸭陪玩」，按 §2.0 不删不改。
- 自动补 6 张 WebP 封面（0.04 MiB）；构建结果 **595 页 / 596 HTML 链接全可解析**，博主模块合集卡 90 → 92，浏览器实测新合集与 3 篇详情坏图 0。

---

## 16. 2026-09-22 全量重跑与孤儿划线修复

### 16.1 孤儿章节划线归位（用户零容忍，头号修复）

- **缺陷**：部分划线（如灿烂千阳「献词」「第二十三章」「第四十九章」）在 `bookmarklist.updated` 中已不存在（前置章/重排后丢失），但其想法（review）仍带 `abstract`（原划线文本）+ range + chapterUid。旧生成器把这些想法当 detached 塞进「章节点评与书评」，只输出 blockquote 摘要 + 想法时间，丢失了它本来就是一条划线的身份、没有 ⏱ 划线时间。
- **修复**（`import-weread-export.mjs` / `repair-weread-notes.mjs` 同源）：把「有 range 但挂不上真书签」的想法按 `(chapterUid, range)` 聚成**隐式划线**——📌=abstract 锚文、⏱=该锚点最早想法时间（划线时刻唯一可得近似）、每条想法=💭+🕰。按 chapterUid/章节名归位进 `# 高亮划线`。noteCount/reviewCount 改为**按渲染模型派生**（恒等式 noteCount==📌、reviewCount==💭+独立h2 条目数）。
- **验证**：灿烂千阳 bookId=567360 修复后 noteCount=29（25 真书签+4 隐式）、reviewCount=23；献词/23/49/44 章 4 条隐式划线全部带 ⏱+💭+🕰 双时间戳。

### 16.2 微信读书统计归档到网络博主合集

- 统计 md 从 `微信读书/微信读书阅读统计.md` 移至新目录 `微信读书统计/微信读书阅读统计.md`（仓库根）。新集合默认 kind=creator → 出现在「网络博主」模块；书架过滤 `collectionName==='微信读书' && kind==='book'` 自动排除。
- `import-weread-export.mjs` 新增 `statsDirectory` 常量，import 时自动写到新路径并清理旧位置。
- 验收：书架网格不出现统计；博主模块有「微信读书统计」合集卡，点进去仅此一篇。

### 16.3 读者评论保全与排版定性

- 大猿取经《扎心的真相》桌面 txt 结尾读者评论（夏豪/广东/6月4日/141/长评）在入库时被整段删除，已补回。
- **排版定性（用户当天二次明确）**：读者评论是网络博主文章**正文的一部分**，初次补回时用的 `---` + `## 读者评论` + bullet + blockquote 特殊栏目做法被用户否决，已改为普通正文段落：四行元数据（夏豪/广东/6月4日/141）用硬换行保持分行，长评成普通段落，无分割线、无标题、无特殊字体；五项信息一字不丢。规则固化进 §7.1 第 8、9 条。

### 16.4 同步数字留痕

- 书架 293 条（books=292+mp=1），notebooks 254 本有笔记，union 307 本逐本拉取；import 更新 294 本、新增 13 本；统计 25 个月（2024-09 ~ 2026-09），累计 102 小时 35 分钟。
- 5 本 CB_/MP_WXS 书 reviews 端点持续 HTTP 499（服务端侧限制，非本地问题）：其**既有想法已从 git 旧版（a9f2c72）按并集全部找回**，并由 §4.5 fail-closed 保护，残缺数据无法再覆盖旧内容；仅这些书**未来新增**的想法需等服务端恢复后重跑同步补齐。
- 最终书库（返工 + 兜底删副本后，见 §16.6）：**301 本书 md**（另 `微信读书Gallery.md` 1 篇不进书架），全库通查恒等式 noteCount==📌 / reviewCount==💭+h2 **0 不符**；📌 总划线 **7096** 且每条带 ⏱、💭 总想法 **2048** 且每条带 🕰，**时间戳 0 缺失**；相对 a9f2c72 并集口径 **0 回退**。

### 16.5 构建结果

- 最终构建（删净第三个白鹿原副本后）：sync-assets **465 个图片（7.88 MiB）**；astro check **0 error / 0 warning**；astro build **638 页**；check-built-site **639 个 HTML 站内链接资源全可解析**。
- dist 实测：书架 **301 张卡**与数据层一致；「白鹿原」全站仅 1 张书卡、1 个详情页（30 📌 / 4 💭，34 个时间戳齐全）；「微信读书统计」合集仅 1 篇；坏图 0。

### 16.6 两轮返工记录（重导冲回合并 → 并集找回 → 第三副本兜底）

1. **第一轮返工**：首轮全量重导后未重跑合并，§15 的 6 组重名书被 CB_ 副本冲回、8 本书划线/想法回退。已重跑 6 组合并（基准：白鹿原 30/4、巨婴国 23/1、研究生论文写作 49📌+3 条独立读书笔记、社会学与生活 1、中县干部 25、国史大纲 5 含简体序言「凡读本书请先具下列诸信念」），8 个文件的丢失内容用 `git show a9f2c72:<path>` 按并集找回，import 加 fail-closed（§4.5、§12.7）。
2. **第二轮兜底（独立验收发现）**：白鹿原还藏着**第三个副本** `微信读书/下载白鹿原201209.md`（bookId=CB_6fC4CQ4DK4tT75j75AGX15Uq，6 划线，进度 45%）——其文件名与书名「白鹿原」毫无共同特征，前两轮按书名/文件名扫描全部漏网。逐字比对确认其 6 条划线已全部包含在幸存者《白鹿原.md》中（纯重复、0 独特内容）后删除；并给 repair 脚本加 §12.7 第 4 条的磁盘兜底删除（按 frontmatter bookId 而非文件名）。
3. **教训固化**：合并验收必须「按 bookId 全库扫描 + 按书名作者聚类」双保险；git 中文路径操作必须带 `-c core.quotepath=false`，否则 diff 列表为空造成假通过。

---

## 17. 2026-09-22 晚：140 字怀疑线全站通查与拆分

### 17.1 规则入册（§7.1 第 10 条）

- 用户强设定**段落怀疑线 = 140 个中文字**（只数汉字，不含标点、空白、西文、标签）：一个代码行上的中文正文超过 140 字即判可疑，必须在自然边界拆成 ≤140 的多段；断点五级优先级、无自然边界原样保留并列报、绝不硬切；拆分只增段落边界、逐字守恒。
- 适用范围：**网络博主模块所有正文段落**（含读者评论、统计合集散文）；**我的书架只查 💭 想法与「章节点评与书评」独立笔记**；**📌 划线原文永远不拆**。

### 17.2 一次性迁移结果（数字留痕）

- **网络博主模块：238 个 md 全部改写落盘**，独立验收（独立解析 + 对 `git show HEAD:<path>` 剥标签剥空白守恒）：超长残留 0、守恒失败 0、行内 HTML 标签完整性问题 0。典型：扎心文夏豪五项元数据硬换行完好；于丹 underline-span 149 字长行正确拆分且标签闭合重开；红与黑 142 字段、爱历史的老丁跨多句高亮段均为「闭合 + 下一段重开」。
- **我的书架模块：131 个 md 改写**（首轮 129 + 旧格式补扫 2 类共 10 文件，幂等）。独立验收：标记总数 **📌 7096 / ⏱ 7096 / 💭 2048 / 🕰 2048 / 记录于 81** 全部不变，💭→🕰、📌→⏱ 配对失败 0，超长残留 0，对 HEAD 守恒失败 0。
- 长想法的目标格式：同一条目内 `> 💭 第一段` → 裸 `>` → `> 第二段`，💭/🕰 各只出现一次、条目数不变；手机端源文的单换行与 3+ 连续空格都视为段落边界。
- **坑（旧格式兼容）**：少数想法在旧版里已是「`> 💭 …` + 裸 `>` + `>    续段」的多段格式（如《九诗心》185 字、《小王子》152 字），首轮迁移器在裸 `>` 处停止收续行导致续段漏拆；修复为裸 `>` 作为段分隔**继续收集到下一个 marker**，迁移幂等可重跑。

### 17.3 防回退：生成器内置同一分段逻辑

- 新增 `site/scripts/seg140.mjs`（JS 版分段器，导出 `hanLen/splitLong/sourceParagraphs/thoughtLines/noteBody`，`splitLong` 内置剥标签守恒断言），与 Cahier `_tools/seg140.py` 规则同源；已用与 Python 版相同的用例对拍（152→132/20、185→131/54 等完全一致）。
- `import-weread-export.mjs`、`repair-weread-notes.mjs` 改为：💭 想法走 `thoughtLines`（多段 blockquote）、独立笔记走 `noteBody`（空行分段）、📌 划线仍走不拆分的 `quoteBlock/quoteLines`。**以后改分段规则只准改 seg140 一处。**
- 顺手修统计生成器旧 bug：`manifest.finishedAt` 为 Unix 秒时 `new Date(秒)` 渲染成 1970 年（新增 `formatFinishedAt` 兼容秒/ISO）；现存统计 md 的错误行 `1970/1/22 01:14:35` 已更正为本次同步提交时刻 `2026/9/22 19:18:42`（原始 finishedAt 已随临时导出目录删除，无法精确复原，故取提交时刻）。

---

## 18. 2026-09-23：读书笔记色块列表支持、统计页时区与日均修复、构建管道挂死事件

### 18.1 读书笔记色块：机制级缺陷修复（81 个笔记组全量复核）

- **缺陷**：`remark-obsidian.mjs#transformWereadNotes` 旧逻辑只向后收集**连续段落**，遇 list 即停，且要求紧随其后必须是含「记录于」的 blockquote，否则整组放弃挂类。含列表的读书笔记因此整块失色。
- **全库扫描结果（301 本）**：共 **81 个读书笔记组**，其中 **8 个含列表组**——《基督教要义（套装全3册）》《思考，快与慢（第二版）》《一口气读完50本经济学巨著》《我看见的世界：李飞飞自传》《稀缺：我们是如何陷入贫穷与忙碌的》《简爱（世界十大文学名著）》《费曼学习法》《长安的荔枝（同名影视原著）》；**1 个含 `---` 组**（《柏杨回忆录》2 处）；**3 个空笔记组**（《倦怠社会》《开国之惑》《月子：当五个女孩成为妈妈》，仅时间戳无正文，维持裸 blockquote 原样）。
- **修复（展示层，未动任何微信读书 md 原文）**：
  1. 按 h3「读书笔记 / 本书评论」标题**整组分组**；组尾必须是含「记录于」的 blockquote 作 meta，否则整组不动（fail-safe）；
  2. 段落与列表全部挂 `note-card__body`，列表额外挂 `note-card__body--list`；首块挂 `note-card__head`（CSS 出 📚 徽标）；
  3. 按 `---` 切视觉段，每段首块 is-first、末块 is-last（末段与 meta 紧邻时末块不挂 is-last、meta 不挂 is-first，由 meta 收口）；
  4. 段落（含列表项内段落）继续走 hardenSoftBreaks 保留手写换行；出现未支持块型时整组放弃。
- **CSS（article.css）**：`.note-card__body` 底色由 `muted/0.45` 加深为 `muted/0.8` 并加 1px 同色系边框（左右常驻、首尾块补上/下边），圆角 10px 配对；新增 `.note-card__body--list` 列表样式（padding 与段落对齐、li>p margin 归零、br 分行）；`.note-card__meta` 配套同色收口。
- **验证**：全库 301 本跑完整 remark 管线，81 组挂类正确、首尾圆角配对正确、修复前后纯文本（去空白）**逐字守恒**；浏览器目视两本点名书 + 统计页全部正常。

### 18.2 阅读统计页三处缺陷（时区口径，2026-09-23 用户发现）

- **总览日均为 0**：总览 API 回包无 `dayAverageReadTime` 字段，生成器直接取值渲染成「0分钟」。修复：总览/逐月日均**一律按「时长 ÷ 阅读天数」现算**（104h11 ÷ 162 天 = **38 分钟**），不再信任 API 字段（逐月字段口径偏小，如 2026-08 实算 38 分、字段仅 14 分）。
- **历年表整体早标一年**：年键是北京 1 月 1 日 0 点（= UTC 上年 12-31T16:00），`getUTCFullYear()` 取成上一年，且真实当年（2026）消失。修复：年键显式 `new Date(ts*1000 + 8h).getUTCFullYear()`。正确结果 **2024 = 7小时29分、2025 = 55小时10分、2026 = 41小时21分**。
- **每日表日期整体早一天**：`formatDate` 旧用 `toISOString().slice(0,10)`（UTC），北京凌晨对应 UTC 前一天，每月每日表都吞入上月最后一天（注册当天 2024-09-04 被标成 09-03）。修复：`formatDate` 统一改用 `Intl.DateTimeFormat('en-CA', {timeZone:'Asia/Shanghai'})`。该函数同时用于书籍 frontmatter 的 readingDate/finishedDate/lastReadDate/注册时间，一并修正。
- **通用时区规则（固化）**：微信读书所有日键 / 年键都是北京时区零点，**任何日期 / 年份渲染必须按 Asia/Shanghai**；跨环境构建用「+8h 偏移取 UTC」保证不依赖构建机器时区。

### 18.3 构建管道挂死事件（2026-09-22 晚，卡 6 小时）

- **事件**：以 `astro build 2>&1 | Select-Object -Last 12 | Out-String` 启动后台构建后，PowerShell、桌面自动化、Grep/Glob/Read、TaskStop、interaction 全部超时，持续约 6 小时；用户手动结束 node 进程后恢复。插件本身已被全库直跑证明不可能挂起（有限遍历、只挂 data），判断为 **Select-Object 后台管道死锁占住工具调度**。
- **规则（固化，违反必重蹈覆辙）**：
  1. astro check / build / 运维脚本输出**一律重定向到文件**（`*> xxx.log`），完成后 `Get-Content xxx.log -Tail N` 读结论；**禁止 `2>&1 | Select-Object` 管道**，尤其在后台任务中；
  2. 命令超时自动转后台后用 TaskOutput block 等待，不重复发起；
  3. 若全通道超时且确认非机器负载，立即请用户任务管理器结束 node / PowerShell，不空等数小时。
- 本次正常构建耗时：sync-assets（465 图 / 7.88 MiB）→ check（0 error / 0 warning / 26 hints）→ build（**638 页 / 约 40 秒**）→ check-built-site（**639 HTML 全可解析**）。

### 18.4 ★每次更新「我的书架」标准 SOP（用户钦定流程，照做即可）

> 触发语：「更新书架」「同步微信读书」「把我最近的笔记导进来」。全程在 `<SITE>` 下、用 `& "C:\Program Files\nodejs\node.exe"`（v24），pnpm 不可用。

```text
① 抓全：按 §4 跑微信读书全量同步（shelf/sync → notebooks 翻页 → 每本 4 接口
   → readdata 总览+逐月），export-manifest 的 failureCount 必须 = 0（失败补抓到 0）。
② 导入：node .\scripts\import-weread-export.mjs <.weread-export 绝对路径>
   记录「更新 X / 新增 Y / 统计 N 个月」；fail-closed 会保护残缺数据不覆盖旧文件。
③ 重跑合并（全量重导会冲回 §15 的 6 组合并结果，必做）：
   node .\scripts\repair-weread-notes.mjs --write
   验收按 frontmatter bookId 全库扫描（不按文件名），确认无 CB_ 副本复活。
④ 数据校验（§11.1）：noteCount==📌、reviewCount==💭+「章节点评与书评」h2 条目数，
   0 不符；分类兜底全量模拟，0 未分类。
⑤ 封面：node .\scripts\generate-missing-covers.mjs（统计页每次被覆盖、需补回），
   再跑 --check 确认全部完整。
⑥ 构建四步（§6.1，输出重定向到日志、禁用 Select-Object 管道）：
   sync-assets → astro check（0 error/0 warning）→ astro build → check-built-site。
⑦ 浏览器冒烟（§6.3）：首页书卡数=书库本数、分类/搜索/模块切换；
   抽查 1–2 本详情页（📌/💭 数、坏图 0）；统计页总览/历年/逐月正确。
⑧ 收尾：删除 .weread-export 与 …\obsidian-weread-plugin-main 缓存树及全部临时脚本。
⑨ 交付：git add -A 核对暂存清单 → commit（写清更新数/统计数字/页面数）
   → 直接 git push origin main（已授权），回报远程推进结果。
⑩ 交付说明写清：书架总数、更新/新增数、累计时长、统计月份范围、未处理项。
```

---

## 附：一次标准任务的最小动作序列（速查）

- **只改了几篇普通文章**：跑 `generate-missing-covers --check` 确认封面全绿（有缺则先补全）→ 四步构建 → 浏览器看集合页 → 交付。
- **同步微信读书**：抓全（manifest 归零）→ import → §11 数据校验 →（需要则 §5 修复）→ 四步构建 → 浏览器冒烟 → 清临时目录 → 交付。
- **修一本丢想法的书**：取 bookId → 4 接口建**只含它**的缓存 → repair `--dry-run` → `--write` → 转 LF → 删缓存树 → 校验 → 构建 → 详情页核对 → 交付。
- **合并一组重名书**：定 survivor/副本 →（必要时泛化 repair 合并逻辑）→ 齐缓存 → dry-run 核对章节/去重数/想法数/lastReadDate → write、删副本 → 转 LF → 校验书架唯一 → 构建 → 搜索该书名确认只剩 1 张卡 → 交付并说明。

> 维护建议：每次新增一类操作或踩一个新坑，就把它补进本手册第 5/11/12 章，让下一次执行无需重新摸索。
